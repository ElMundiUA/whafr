__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
.env
.env.local
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.DS_Store
*.swp
*.swo
.idea/
.vscode/
# Ingested content + raw scratch (markdown, dumps). Source-research
# YAMLs + the periodic coverage-audit markdown are *config*, not
# ingested content — keep them tracked.
data/*
!data/source-research/
data/source-research/unparseable.jsonl
*.db
*.dump

# Internal planning docs — not for public repo
RELEASE_PLAN.md

# Raw bench outputs (per-task JSON + intermediate logs). Summary PDFs
# in ``reports/`` are the public artifact; raw runs are too noisy to
# track in git.
tools/eval/
